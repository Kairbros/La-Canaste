# URL base de la API del backend (incluye /api).
# En desarrollo puedes dejarlo apuntando al backend local.
# En producción, ponlo a la URL pública del backend, p.ej:
# VITE_API_URL=https://canasta-backend.onrender.com/api
VITE_API_URL=http://localhost:4000/api
